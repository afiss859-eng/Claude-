name: Build Burkina Market APK

on:
  push:
    branches: [main]
    paths:
      - "apps/mobile/**"
      - "package.json"
      - "pnpm-workspace.yaml"
      - ".github/workflows/android-apk.yml"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  android-apk:
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: bash
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10.15.0

      - name: Install dependencies
        run: pnpm install --no-frozen-lockfile

      - name: Generate Android project
        working-directory: apps/mobile
        run: npx expo prebuild --platform android --non-interactive

      - name: Build installable APK
        working-directory: apps/mobile/android
        run: ./gradlew assembleDebug --no-daemon

      - name: Prepare APK
        run: cp apps/mobile/android/app/build/outputs/apk/debug/app-debug.apk Burkina-Market.apk

      - name: Upload workflow artifact
        uses: actions/upload-artifact@v4
        with:
          name: Burkina-Market-apk
          path: Burkina-Market.apk
          if-no-files-found: error
          retention-days: 30

      - name: Publish stable GitHub Release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          if gh release view android-latest >/dev/null 2>&1; then
            gh release upload android-latest Burkina-Market.apk --clobber
          else
            gh release create android-latest Burkina-Market.apk               --title "Burkina Market APK"               --notes "APK Android compilé automatiquement depuis la branche main."
          fi
